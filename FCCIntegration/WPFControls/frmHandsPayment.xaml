<Window x:Class="FCCIntegration.WPFControls.frmHandsPayment"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="540" Width="540" Height="300"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        >
    <Grid>
    <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>
        <TextBlock Name="LblMsg" FontSize="25" TextAlignment="Center" HorizontalAlignment="Stretch" TextWrapping="Wrap" Text="Вы уверены,что хотите закрыть чек без исользования CashInfinity? " Grid.Row="0"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button Name="btnNal"  Content="Наличные" FontSize="25" Click="btnNal_Click" Margin="10" Width="150"/>
            <Button Name="btnCard" Grid.Row="1" Content="Карта" FontSize="25" Click="btnCard_Click" Margin="10" Width="150"/>
            <Button Name="btnCancel" Grid.Row="1" Content="Отмена" FontSize="25" Click="btnCancel_Click" Margin="10" Width="150"/>
        </StackPanel>
    </Grid>
    
</Window>
